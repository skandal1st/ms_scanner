[pytest]
# Тесты гоняем из каталога backend/ — так пакет app импортируется без установки.
pythonpath = .
testpaths = tests
asyncio_mode = auto
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
