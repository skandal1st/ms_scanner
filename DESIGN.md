# Design

<!-- impeccable:design-schema 1 -->

Визуальный язык МС-Сканера — «складская консоль маркировки». Вдохновлён новым
МойСкладом (плоский, азурно-светлый), но со своей айдентикой: чернильно-синяя
шапка + один кобальтовый акцент действия. Режим — **Operate** (кладовщик
выполняет задачу; считываемость и консистентность важнее выразительности).

Источник истины — код: `frontend/src/styles/uikit.css` (токены + базовые
компоненты) и `frontend/src/styles/app.css` (компоненты приложения). Токены
сохраняют имена `--ms-*` (значения обновлены), поэтому весь код на них
подхватывает язык автоматически.

## Цвет

Стратегия — **Restrained** (нейтрали + один акцент), как и положено рабочему
инструменту.

- **Фон страницы** `--ms-bg-alt #EDF0F6` (прохладный светлый). **Поверхности**
  `--ms-bg #FFFFFF`, мягкая заливка `--surface-2 #F5F7FB`.
- **Бренд-кобальт** (акцент действия): `--brand #2C5CE5`, hover `--brand-strong
  #2049C4`, заливка чипов/активных `--brand-weak #E7EDFC`, hover строк
  `--brand-tint #F2F6FE`, фокус-ринг `--brand-ring rgba(44,92,229,.22)`.
- **Чернильная шапка**: `--chrome #17233D`, `--chrome-2 #223251`,
  `--chrome-line #2C3A58`. Отличает продукт от мид-синей полосы МС.
- **Текст**: `--ms-text #1E2634`, `--ms-text-muted #5A6474`,
  `--ms-text-subtle #8B94A6`.
- **Границы**: `--ms-border #CFD6E4`, `--ms-border-light #E3E8F2`,
  `--ms-input-border #CBD3E1`.
- **Статусы (функциональны, палитра МС-семейства)** — плоские пилюли `fg/bg/bd`:
  ok зелёный `#2E7D32`, warn оранжевый `#B7590A`, err красный `#C0322B`,
  muted серый `#5B6472`, info кобальт `#2049C4`. Используются и в бейджах, и в
  подсветке строк сканов (валид/брак/сверх/выведен/несопоставлен/чужой владелец).
- Светлая тема — из сцены использования: склад, яркий свет, работа стоя, десктоп.

## Типографика

- **Face**: `HelveticaNeueCyr` (самохост, `public/uikit/fonts/*.woff`) — рабочий
  нейтральный гротеск. Стек: `'HelveticaNeueCyr', 'Helvetica Neue', Helvetica,
  Arial, 'Segoe UI', sans-serif`. Моно для кодов: `--ms-font-mono`.
- **Масштаб**: body 13px/1.45; h1 22/700 (tracking −0.02em), h2 16/600, h3 13/600.
  Заголовки страниц потоков — 19px/700.
- **Табличные цифры** (`font-variant-numeric: tabular-nums`) на всех таблицах,
  счётчиках и прогрессе — колонки не «прыгают».

## Форма и глубина

- **Радиусы**: `--r-sm 5px` (поля, бейджи-точки), `--r-md 8px` (кнопки, карточки),
  `--r-lg 12px` (модалки, карточки-контейнеры), `--r-pill 999px` (чипы, бейджи,
  прогресс-бары).
- **Тени** (смещение + мягкий блюр): `--shadow-1 0 1px 2px rgba(16,24,40,.06)`,
  `--shadow-2 0 4px 16px rgba(16,24,40,.10)`, `--shadow-pop 0 18px 48px
  rgba(16,24,40,.24)`. Никаких нулевых цветных гало.
- **Движение**: экспоненциальный ease-out; один осмысленный момент —
  `.done-overlay` (fade + мягкий scale чек-иконки, `cubic-bezier(0.22,1,0.36,1)`).
  Прогресс-бар плавно заполняется (width/color 0.25s).

## Компоненты

- **Кнопки**: плоские. `.button` — белая с 1px границей, hover `--surface-2`,
  фокус кобальт-ринг. `.button--primary`/`.button--success` — сплошной кобальт
  (основная CTA: «Отгрузить/Принять/Сохранить»). `.button--danger` — красная.
  `.button--sm` — компактная. `.button--link` — текстовая.
- **Поля/select**: высота 34px, радиус `--r-sm`, фокус — кобальт + ринг; стрелка
  select — инлайн-SVG-шеврон. Чекбокс/радио — плоские, кобальтовая заливка.
- **Скан-поле** `.scan-input__field` — крупное (44px), моно, кобальтовая рамка с
  постоянным рингом (визуально «в фокусе для сканера»).
- **Таблицы**: шапка — `--surface-2`, мелкий uppercase-лейбл; строки —
  волосяные разделители, hover `--brand-tint`, ссылки-номера кобальтом.
- **Бейджи** `.badge--*` — плоские пилюли статусов (+ `.badge__dot`).
- **Чипы** `.chip` — пилюли-фильтры в бледном кобальте (сигнатура МС).
- **Алерты** `.alert--error/ok/warn/info` — единый стиль сообщений.
- **Модалки** `.popup__body` — радиус `--r-lg`, `--shadow-pop`, оверлей с blur;
  крестик — маска-SVG.
- **Верхняя навигация** `.app-nav` — чернильная шапка (52px): лого-марка +
  разделы с инлайн-SVG-иконками, активный — подсветка + кобальтовое подчёркивание.
- **Оверлей результата** `.done-overlay` — «Отгружено/Приёмка отправлена» с
  зелёной чек-иконкой.

## Иконки

Авторский линейный SVG-набор `components/Icon.tsx` (stroke 1.7, currentColor,
единый штрих): shipment, acceptance, writeoff, check, settings, logout, scan,
plus, close, filter, refresh, chevron, upload, link, trash. Эмодзи как иконки
в чроме заменены; остаются лишь единичные предупреждающие маркеры в тексте.

## Принципы рендера

1. Считываемость статусов на потоке — цвет статуса читается за доли секунды.
2. Единство с МС без шва, но со своей шапкой и акцентом.
3. Плоско, без скевоморфных градиентов (антиреференс — старый UI-кит МС).
4. Табличные цифры и стабильные колонки для потоковой работы.
